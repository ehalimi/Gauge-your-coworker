<div class="employee-list" >
    <ul   class="row justify-content-center">
        {{#each employees as |employee|}}
        <li class="col-12 col-lg-7 employee-section">
            {{> employee-info employee}}
        </li>
        {{/each}}
    </ul>
</div>